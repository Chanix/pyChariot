import{_ as p,c as s,o as n,ae as i}from"./chunks/framework.BLKehFbR.js";const _=JSON.parse('{"title":"文件目录结构","description":"","frontmatter":{"layout":"doc"},"headers":[],"relativePath":"dirs-and-files.md","filePath":"dirs-and-files.md"}'),e={name:"dirs-and-files.md"};function t(o,a,c,l,d,h){return n(),s("div",null,a[0]||(a[0]=[i(`<h1 id="文件目录结构" tabindex="-1">文件目录结构 <a class="header-anchor" href="#文件目录结构" aria-label="Permalink to &quot;文件目录结构&quot;">​</a></h1><p>pyChariot 被设计为方便迁移的绿色软件，所有运行需要的文件都保存于 <code>pyChariot 主目录</code> 之下。</p><p>由于这样的设计，迁移、删除或者复制您的 pyChariot 环境，只需对此目录进行文件操作即可：</p><ul><li>使用时，可以保存在移动存储设备（例如移动硬盘）上，即插即用;</li><li>复制时，完整复制目录即可，与原环境完全一致;</li><li>删除时，删除目录即可，不留任何垃圾。</li></ul><h2 id="pychariot-home" tabindex="-1">pyChariot_HOME <a class="header-anchor" href="#pychariot-home" aria-label="Permalink to &quot;pyChariot_HOME&quot;">​</a></h2><p>一般来说，pyChariot 可执行文件的保存位置，即为 <code>pyChariot 主目录</code>，即 <code>pyChariot_HOME</code>。</p><p>以 Windows 为例：pyChariot 可执行文件解压保存在 <code>D:\\pyChariot</code> 下，则 <code>pyChariot_HOME</code> 的值为 <code>D:\\pyChariot</code> ，则所有运行需要的文件都保存在这个目录之下。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    pyChariot_HOME              # D:\\pyChariot</span></span>
<span class="line"><span>        |   pyChariot.exe       # 文件：可执行文件，运行环境</span></span>
<span class="line"><span>        |   pyChariotKit.exe    # 文件：可执行文件，开发调试环境</span></span>
<span class="line"><span>        +---packages            # 目录：外部工具目录</span></span>
<span class="line"><span>        |</span></span>
<span class="line"><span>        +---temp                # 目录：临时文件目录</span></span>
<span class="line"><span>        |</span></span>
<span class="line"><span>        +---data                # 目录：应用数据目录</span></span>
<span class="line"><span>        |</span></span>
<span class="line"><span>        +---wvdata              # 目录：浏览器组件数据目录</span></span>
<span class="line"><span>        |</span></span>
<span class="line"><span>        \\---appchips            # 目录：应用目录</span></span></code></pre></div><p>在这些目录下，再按照应用标识建立子目录，存放相应文件。</p><p>例如，有个项目，项目标识为 <code>{appchip_id}</code>，则：</p><ul><li>应用的程序文件目录： <code>pyChariot_HOME/appchips/{appchip_id}</code>；</li><li>应用的临时文件目录： <code>pyChariot_HOME/temp/{appchip_id}</code>；</li><li>应用的数据文件目录： <code>pyChariot_HOME/data/{appchip_id}</code>；</li><li>应用的浏览器组件数据目录： <code>pyChariot_HOME/data/{appchip_id}</code>；</li></ul><p>这样约定的是目的是为了更便捷和统一的管理应用的各种文件。使用中，请按照这个约定来存储对应的文件。</p><h2 id="pychariot-home-appchips" tabindex="-1">pyChariot_HOME/appchips <a class="header-anchor" href="#pychariot-home-appchips" aria-label="Permalink to &quot;pyChariot_HOME/appchips&quot;">​</a></h2><p>所有应用都存放在 appchips 下，以应用标识作为其存放目录名称，其结构如下：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    appchip_id                  # 目录名即应用的标识，请尽量使用英语字符。</span></span>
<span class="line"><span>        |   app.json            # 应用的描述文件</span></span>
<span class="line"><span>        |                       #   详细记录了应用的相关信息，必须存在。</span></span>
<span class="line"><span>        |                       #   当 pyChariot 启动项目时会读取该文件。</span></span>
<span class="line"><span>        |</span></span>
<span class="line"><span>        |   app.ico             # Windows 操作系统下，应用的窗口图标（可选）。</span></span>
<span class="line"><span>        |</span></span>
<span class="line"><span>        +---js                  # 应用中使用的一些本地 JavaScript。</span></span>
<span class="line"><span>        |</span></span>
<span class="line"><span>        |</span></span>
<span class="line"><span>        \\---webroot             # 应用 httpd 使用的文件</span></span>
<span class="line"><span>                                #   本地 httpd 服务器将以本目录作为 / 。</span></span></code></pre></div><p><em><br>运行于 pyChariot 上的应用，称为 <code>appchip</code>。嗯……本来想叫 “appclip”的，打错了……不过无论是 “薯片”还是 “芯片”，我都喜欢，就叫 <code>appchip</code> 吧 😃</em></p><h2 id="pychariot-home-wvdata" tabindex="-1">pyChariot_HOME/wvdata <a class="header-anchor" href="#pychariot-home-wvdata" aria-label="Permalink to &quot;pyChariot_HOME/wvdata&quot;">​</a></h2><p>浏览器组件数据存放目录。</p><p>浏览器组件数据和系统浏览器数据是隔离的，因此应用的数据不会和系统浏览器中的数据相互影响（例如登录数据，Cookie数据）。 这样可以尽最大可能的保证独立性。</p><p>因此，应用第一次运行时会略微慢一些（需要重新生成和保存这些数据）。同样基于此，不建议将 pyChariot 保存在劣质 U 盘中，因其读写可靠性甚至差于移动机械硬盘。</p>`,20)]))}const y=p(e,[["render",t]]);export{_ as __pageData,y as default};
